import { School } from '@tamagui/lucide-icons';
import { Dispatch, SetStateAction } from 'react';

export type LucideIcon = typeof School;
export type ReactSetStateType<T> = Dispatch<SetStateAction<T>>;
